import React from 'react';
import PropTypes from 'prop-types';

const App = props => {
  return (
    <div className="ui container">
      App!
    </div>
  );
};

App.propTypes = {

};

export default App;
